# config.py
file_extension = "cxx"        # e.g. "cxx" or "myext" (no leading dot)
attributes_dir = "./attrib"         # where your attribute Python files live (default ".")
target_dir = "."             # directory where to find the .{file_extension} files (default ".")
recursive = True            # set True to descend into subfolders for C++ files
